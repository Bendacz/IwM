package projekt;

public abstract class Packet {
protected String device = "dev";
protected String description = "desc";
protected long date = 444;

public Packet(String device, String description, long date) {
	this.device = device;
	this.description = description;
	this.date = date;
	System.out.println("costam");
}

public String toString(String device, String description, long date) {
	System.out.println(device + description + date);
	//return(device, description, date);
}
}